---
import Button from '@/components/common/Button.astro';

const consoleUrl = import.meta.env.CONSOLE_URL || 'https://console.kubeletto.com';
---

<section class="relative overflow-hidden py-20 sm:py-32">
  <!-- Background Orb (The Glow) -->
  <div class="absolute top-0 right-0 w-96 h-96 bg-kubeletto-blue opacity-20 blur-[120px] rounded-full animate-orb-glow -z-10"></div>

  <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-[1200px]">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Left: Content -->
      <div class="space-y-8">
        <h1 class="text-display sm:text-[48px] lg:text-[56px] leading-tight">
          Deploy anywhere. Focus on code.
        </h1>
        <p class="text-lg sm:text-xl text-muted-foreground max-w-[600px]">
          Kubeletto handles Kubernetes complexity so you don't have to. From git push to production in 60 seconds.
        </p>

        <!-- CTAs -->
        <div class="flex flex-col sm:flex-row gap-4">
          <Button href={`${consoleUrl}/sign-in`} variant="primary" size="lg">
            Start Free Trial
          </Button>
          <Button href="/docs" variant="secondary" size="lg">
            View Docs
          </Button>
        </div>

        <!-- Trust Indicators -->
        <div class="flex flex-wrap gap-6 pt-8">
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            <span class="text-sm text-muted-foreground">99.9% uptime</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            <span class="text-sm text-muted-foreground">SOC 2 compliant</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            <span class="text-sm text-muted-foreground">10,000+ deployments</span>
          </div>
        </div>
      </div>

      <!-- Right: Hero Image -->
      <div class="relative">
        <div class="relative rounded-lg border border-border overflow-hidden shadow-[0_12px_32px_-4px_rgba(0,0,0,0.2)]">
          <img
            src="/assets/images/PLACEHOLDER-hero-dashboard-screenshot.png"
            alt="Kubeletto dashboard showing deployment pipeline"
            width="1200"
            height="675"
            class="w-full h-auto"
          />
        </div>
        <!-- TODO: Replace with actual dashboard screenshot -->
      </div>
    </div>
  </div>
</section>
